version: 1.0
description: Install the main branch
input:
  - github_data
tasks:
  install_main:
    action: packs.install
    input:
      packs:
        - "https://github.com/nufuk/nufuk.git"
    retry:
      when: <% failed() %>
      count: 3
      delay: 30
