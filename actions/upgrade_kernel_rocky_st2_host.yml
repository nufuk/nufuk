---
name: upgrade_kernel_rocky_st2_host
description: trigger the Rocky Kernel upgrade for the st2 host
runner_type: orquesta
entry_point: workflows/kernel_upgrade_rocky.yml
enabled: true
parameters:
  host:
    required: true
    type: string
    default: "{{ st2kv.system.st2_host }}"
